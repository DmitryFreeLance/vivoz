# Телеграм бот «Вывезем Всё»

## Что внутри
- Java 17 + `telegrambots 6.9.7.1`
- SQLite для хранения заявок
- Inline-кнопки с эмодзи
- Админ-панель (просмотр статистики и последних заявок)
- Dockerfile и команда запуска

## Переменные окружения
- `BOT_TOKEN` — токен бота от @BotFather
- `BOT_USERNAME` — username бота (без @), опционально
- `ADMIN_IDS` — id администраторов через запятую (например: `123456789,987654321`)
- `DB_PATH` — путь к БД SQLite (по умолчанию `data/bot.db`)

## Локальный запуск
```bash
mvn -q -DskipTests package
BOT_TOKEN=YOUR_TOKEN \
BOT_USERNAME=your_bot \
ADMIN_IDS=123456789 \
java -jar target/app.jar
```

## Docker
Сборка:
```bash
docker build -t vivoz-bot .
```

Запуск:
```bash
docker run -d --name vivoz-bot \
  -e BOT_TOKEN=YOUR_TOKEN \
  -e BOT_USERNAME=your_bot \
  -e ADMIN_IDS=123456789 \
  -e DB_PATH=/data/bot.db \
  -v $(pwd)/data:/data \
  vivoz-bot
```

## Админ панель
- Открывается через кнопку «🛠 Админ панель» (видна только администраторам) или командой `/admin`.
- Показывает общее количество заявок, заявки за сегодня и последние 5 заявок.

